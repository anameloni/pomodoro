// This file content is present at all app pages. It's updated every time the client upload the page. Used for dynamic content
import '../styles/global.css';

function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />
}

export default MyApp
